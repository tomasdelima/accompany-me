<div class="box">
  <h2 class="expandable" data-target="<%= singular_label %>">
    <i class="glyphicon glyphicon-<%= icon %>"></i>
    <%= t("activerecord.model.#{plural_label}") %>
    <span class="<%= singular_label %> count">(<%= objects.try(:count) %>)</span>
  </h2>

  <ul class="<%= singular_label %> list expandable-list">
    <% objects.each do |object| %>
      <li>
        < %debugger%>
        <%= link_to t("helpers.edit"), send("edit_#{@object.class.to_s.downcase}_path", @object), class: "inline-edit" %>
        <%= link_to send("#{@object.class.to_s.downcase}_path", @object) do %>
         <span class="listed left"><%= object.name %></span>
        <% end %>
      </li>
    <% end %>
  </ul>

  <div>
    <%= link_to t("helpers.submit.create", model: t("activerecord.model.#{singular_label}")), new_path, class: "btn btn-success" %>
  </div>
</div>